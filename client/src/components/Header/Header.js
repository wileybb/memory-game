import React from 'react'
import "./style.css";

function Header() {
    return(
        <header className="header">
            <h1>Memory Game</h1>
            <h2>Click the images to reveal. Try to match all the pictures!</h2>
        </header>
    );
}

export default Header;